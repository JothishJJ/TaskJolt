import { PrimaryOutlinedBtnDirective } from './primary-outlined-btn.directive';

describe('PrimaryOutlinedBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new PrimaryOutlinedBtnDirective();
    expect(directive).toBeTruthy();
  });
});
